# Chrome 版本特性介绍项目

一个零依赖的前端展示页，用来介绍 Chrome 各阶段版本的关键特性、技术路线和平台能力演进。

## 运行方式

直接用浏览器打开 `index.html`，或者在项目目录启动一个静态服务器。

## 项目结构

- `index.html`：页面结构
- `styles.css`：视觉样式与响应式布局
- `script.js`：版本数据、卡片渲染与滚动动画
